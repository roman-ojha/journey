import { APPLICATION_NAME } from "@/constants";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: `Contact | ${APPLICATION_NAME}`,
};

const Contact = (): React.JSX.Element => {
  return <></>;
};

export default Contact;
